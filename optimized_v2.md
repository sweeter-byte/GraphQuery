M1
枚举序列空间 -> Survey: filter + order

输出：序列

M2

代价评估 : 去重 + 直接估计
并行两层 : 外层no + 内层yes  (0,1) (1,0) (1,1) (0,0)

输出： winner order

M3

survey engine  

测量时间：


论文指出，没有一个组合在所有cases下最好。该命题成立，基于所有组合，找出最好的序列。
论文指出，
- 最佳组合随数据集而异，实验显示，特定查询阶段（过滤、排序、枚举）的最佳技术选择会根据图数据的结构特征而改变。
- 最佳组合随输出限制（Output Limit）而异
- 最佳组合随查询特征（如树宽）而异
- 技术间的交互作用（Interaction）导致动态变化

exp：

1. 并行化实验
2. 组合搜索最优序列证明有用


optimized exp:

1. M1, M3 survey 代码 + 数据集 -> (methods_order, 时间)
2. (methods_order, 时间) -> 调整 cost_model weight 
3. 机器学习评估(分类任务) 二分类  相对评估，三角循环、偏序评估

cost = w1m1 + w2m2 + w3m3 + ... + mn


论文：

新指标：引入了 每秒嵌入数（EPS）
- 定义：单位时间内返回的平均嵌入数量。
- 优势：该指标综合考虑了时间成本和结果数量，在不同的时间限制和输出限制下表现出较强的一致性，能更客观地反映算法效率。

实验证明，不同技术之间存在显著的协同或制约关系。
例如，枚举技术 eVEQ 的性能极大地依赖于过滤技术 fVEQ 的配合；如果不考虑这种交互，很难发挥特定算法的真实潜力。