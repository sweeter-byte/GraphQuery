# M2 序列代价估计优化方案

## 0 文献调研与方案合理性评估

基于对子图匹配领域近年文献和开源实现的系统调研，对本方案三项优化的合理性、已知局限和可替代方案做如下评估。

### 0.1 O1（选择性前缀估计）— 合理，但可进一步增强

**文献支撑**：

目前没有论文专门讨论"在查询计划选择阶段跳过共享前缀的基数估计"。但相关理论基础已存在：

- CEMR（arXiv 2603.08037, 2026）提出 Common Extension Reusing：若两个 partial embedding 对某顶点的 backward neighbors 有相同映射，则扩展结果可复用（Lemma 3.1）。该引理的核心思想可迁移到前缀估计场景——共享相同前 $k$ 个顶点的序列，其第 $k$ 个前缀子图的基数估计值相同。
- 关系数据库中的 Common Subexpression Elimination 是成熟技术，在子图匹配的前缀估计场景属于"显然正确的工程优化"。

**评估**：R1（末尾前缀消除）和 R2（首个前缀跳过）是精确无损的，完全合理。R3（自适应跳过）有排序偏移风险，需要实验验证。

**更优方案 — R4：前缀 Memoization**

原方案忽略了一个更强的优化：不同序列可能共享相同的前缀子图。例如序列 $(v_0, v_1, v_2, v_3)$ 和 $(v_0, v_1, v_3, v_2)$ 的前 2 个前缀子图完全相同。将前缀子图的 canonical form（如排序后的顶点集 hash）作为 key 做 memoization，可以跨序列复用估计结果，节省量远超 R1-R3。

实现方式：在 `session_pipeline.py` 的逐层估计循环中，维护 `cache: dict[frozenset[int], float]`，以前缀顶点集合为 key 缓存估计值。

### 0.2 O2（加权代价模型）— 方向正确，但需注意替代方案

**文献支撑**：

- **NeuSO**（arXiv 2509.23775, 2025）将序列优化建模为 Cardinality-Cost Graph（CCG）上的最短路径问题，代价函数为 $C(e_{q_1,q_2}) = C(q_1) \cdot \min_{u \in N(u_0) \cap V_{q_1}} |C(u,u_0)| / |C(u)|$，即通过候选集比率加权基数。这是目前最接近"位置感知加权基数代价模型"的工作。
- **DAF**（SIGMOD 2019）使用自适应匹配顺序，在搜索过程中基于运行时候选集大小动态选择下一个扩展顶点，而非静态预计算代价。
- **RL-Based Query Vertex Ordering**（2022, Papers with Code）使用强化学习自动学习顶点排序策略，奖励信号基于实际搜索空间大小。
- **传统方法**（CFL、DP-iso、RI、VF2++）均未使用显式加权基数代价模型，而是依赖候选集大小的简单启发式。

**评估**：

我们的位置-拓扑加权方案在现有文献中尚属空白，是一个有价值的创新点。但存在以下风险：

1. 手工设计的 $\alpha(k) \cdot \beta(Q_k)$ 可能不如数据驱动方法。NeuSO 的多任务学习框架（同时预测基数和执行代价）在理论上更优，但需要训练数据。
2. 超参数 $(\gamma, \lambda)$ 的最优值可能因数据集而异，泛化性需要实验验证。

**更优方案 — 候选集比率代价模型**

受 NeuSO 启发，可以考虑一种不需要训练的替代代价函数：

$$\text{Cost}(O) = \sum_{k=2}^{n-1} \hat{c}_k \cdot \frac{1}{|C(v_k)|} \cdot \min_{u \in N_Q(v_k) \cap \{v_1,...,v_{k-1}\}} \frac{|C(v_k, u)|}{|C(u)|}$$

其中 $|C(v)|$ 是顶点 $v$ 的候选集大小（可从 FaSTest 的过滤阶段获取），$|C(v_k, u)|$ 是边 $(v_k, u)$ 的候选边数。这比纯位置衰减更有信息量，因为它直接反映了每步扩展的实际过滤效率。

但此方案需要 FaSTest C++ 端暴露候选集大小信息，实现成本较高。建议先实现简单的位置衰减方案，再考虑升级。

### 0.3 O3（高代价预警）— 合理，但需修正对估计精度的预期

**文献支撑**：

- **FaSTest 论文**（arXiv 2309.15433）报告：Human 数据集 16 节点查询平均 q-error 1.09，Yeast 数据集嵌入数超过 $10^{15}$ 的查询平均 q-error 1.10。使用 Clopper-Pearson 置信区间，证明为无偏一致估计量。
- **G-CARE 基准**（SIGMOD 2020）的结论更为警醒："所有现有技术在各种场景和数据集上都存在严重的精度问题"。COLOR 框架揭示在线采样方法在复杂查询上 60-99% 失败率。
- **悲观基数估计 / LpBound**（arXiv 2502.05912, SIGMOD 2025 最佳论文）提供有保证的上界，永远不会低估，适合作为"查询是否会很昂贵"的预警信号。
- **"Can Optimizer Cost Predict Execution Time?"**（arXiv 1905.00774）发现优化器代价与执行时间在对数-对数空间呈幂律关系（$R^2 = 0.56$），线性关系很弱。

**评估**：

原方案中的可信度分级（high: <10%, medium: 10-50%, low: >50%）过于乐观。根据 G-CARE 的结论，FaSTest 在特定数据集上精度很高（q-error ~1.1），但不能假设在所有场景下都如此。

关键修正：
1. 不应将基数估计值作为"精确结果数量"呈现给用户，而应定位为"数量级参考"和"查询难度指标"。
2. 预警机制应使用悲观估计（上界），而非点估计。FaSTest 的 Clopper-Pearson 置信区间上界可直接用于此目的。
3. 代价-时间映射不应假设线性关系，应使用对数空间的幂律模型。

**更优方案 — 渐进式验证**

受 POP（SIGMOD 2004）和 LPCE（SIGMOD 2023）启发，可以在 M3 执行的早期阶段验证基数估计：

1. M3 开始执行后，在前 $t_0$ 秒内观察实际枚举速度
2. 若实际速度与估计值严重不符（如实际嵌入数增长率远超预期），则提前终止并预警
3. 这比纯静态预警更可靠，因为它结合了实际执行信息

### 0.4 文献中发现的额外优化方向

| 方向 | 来源 | 核心思想 | 与本项目的关系 |
|------|------|---------|--------------|
| CCG 最短路径建模 | NeuSO (2025) | 将序列优化建模为图上最短路径，复杂度 $O(|V_Q|^2)$ | 可替代当前的枚举+排序范式 |
| 自适应匹配顺序 | DAF (SIGMOD 2019) | 在搜索过程中动态调整顶点扩展顺序 | M3 层面的优化，与 M2 互补 |
| GNN + Transformer 重排序 | NeuGN (2025) | 用神经网络直接预测最优顶点顺序 | 需要训练数据，但首次匹配步数减少高达 98.2% |
| 悲观基数估计 | LpBound (SIGMOD 2025 Best Paper) | 提供有保证的上界 | 可用于 O3 预警的理论基础 |
| 冗余扩展消除 | CEMR (2026) | 共享 backward neighbor 映射的扩展结果复用 | O1 前缀 memoization 的理论支撑 |

### 0.5 综合建议

1. O1 方案合理，建议增加 R4（前缀 memoization），这是收益最大且零风险的优化。
2. O2 方案方向正确，位置衰减加权在文献中是空白，有创新价值。建议先实现简单版本，用实验数据验证后再考虑是否引入候选集比率等更复杂的信号。
3. O3 方案需要修正对估计精度的预期。建议使用 FaSTest 的置信区间上界而非点估计做预警，并考虑渐进式验证作为补充。
4. 长期来看，NeuSO 的 CCG 建模思路值得关注——它将 M1+M2 统一为一个图上的最短路径问题，从根本上避免了枚举+排序的开销。

---

## 1 现状分析

### 1.1 当前代价模型

M2 阶段对每条候选扩展序列 $O = (v_1, v_2, \ldots, v_n)$ 计算代价：

$$\text{Cost}(O) = \sum_{k=1}^{n} \omega_k \cdot \hat{c}_k$$

其中 $\hat{c}_k$ 是第 $k$ 个前缀子图 $Q_k = Q[\{v_1, \ldots, v_k\}]$ 的基数估计值，$\omega_k$ 是权重系数。当前实现中 $\omega_k \equiv 1.0$（`score_aggregator.py:get_weight`），即所有前缀等权求和。

代价最低的序列被选为最优序列，送入 M3 下游执行引擎。

### 1.2 存在的问题

**P1：冗余前缀估计**

对每条序列的全部 $n$ 个前缀子图都执行基数估计，但其中存在冗余计算：

- **末尾前缀恒等**：所有序列的第 $n$ 个前缀子图 $Q_n$ 都是完整查询图 $Q$ 本身，其基数估计值 $\hat{c}_n$ 对所有序列相同，不影响排序。
- **首个前缀低区分度**：$Q_1$ 仅含单个顶点，其基数估计值主要取决于顶点标签在数据图中的频率，对序列质量的区分能力有限。
- **中间前缀的边际贡献不均**：并非每个前缀的估计值都对最终排序产生实质影响。某些前缀的估计值在所有序列间差异极小，计算它们是浪费。

当前实现中，每条序列需要 $n$ 次 C++ 基数估计调用，总计 $n \times |\Omega|$ 次（$|\Omega|$ 为候选序列数）。若能跳过冗余前缀，可显著降低 M2 耗时。

**P2：等权求和与真实执行代价不一致**

等权求和假设每个前缀子图对序列执行代价的贡献相同，但这与子图匹配的实际执行过程不符：

- **位置效应**：序列前部的顶点决定了搜索树的根结构，对执行时间的影响远大于末尾顶点。早期的高基数意味着搜索树在根部就发生爆炸，后续剪枝难以弥补。
- **拓扑效应**：前缀子图的拓扑结构（如是否形成环、连通度）影响其过滤能力。含环的前缀约束更强，其基数估计值对执行时间的预测能力更高。
- **边际递减**：随着前缀增长，新增顶点带来的约束增量递减（因为大部分约束已被前面的顶点覆盖）。

因此，等权求和得到的"最优"序列在 M3 真实执行中未必最优。

**P3：缺乏高代价查询预警**

当基数估计值极大时，意味着下游查询的执行时间可能极长甚至超时。当前系统缺乏对此的预警机制，用户可能在不知情的情况下触发耗时极长的查询。

## 2 优化方案

### 2.1 O1：选择性前缀估计（Selective Prefix Evaluation）

#### 核心思想

并非所有前缀都需要估计。通过识别和跳过对排序无贡献的前缀，减少 C++ 估计调用次数。

#### 跳过规则

**R1 — 恒等前缀消除**：跳过第 $n$ 个前缀（完整查询图），因为 $\hat{c}_n$ 对所有序列相同。

**R2 — 低区分度前缀跳过**：跳过第 1 个前缀（单顶点），当且仅当查询图中所有顶点标签相同（此时 $\hat{c}_1$ 对所有序列相同）。

**R3 — 自适应跳过**：在逐层估计过程中，若某一层所有序列的估计值方差低于阈值 $\epsilon$，则后续层中该层的贡献可视为常数偏移，不影响排序。具体地：

$$\text{Var}(\hat{c}_k) = \frac{1}{|\Omega|} \sum_{O \in \Omega} (\hat{c}_k^O - \bar{c}_k)^2$$

若 $\text{Var}(\hat{c}_k) < \epsilon \cdot \bar{c}_k^2$（相对方差阈值），则标记第 $k$ 层为"低区分度"，后续序列可跳过该层估计。

#### 预期收益

- R1 直接节省 $|\Omega|$ 次估计调用
- R2 在同标签查询图上再节省 $|\Omega|$ 次
- R3 的节省量取决于查询图结构，预计在规则图上效果显著
- R4 的节省量取决于序列间的前缀共享程度，预计在 Beam Search 模式下效果显著（相邻序列往往共享长前缀）

#### 跳过规则（续）

**R4 — 前缀 Memoization**（基于文献调研新增）：不同序列可能共享相同的前缀子图（如序列 $(v_0, v_1, v_2, v_3)$ 和 $(v_0, v_1, v_3, v_2)$ 的前 2 个前缀完全相同）。以前缀顶点集合的 frozenset 为 key 缓存估计结果，跨序列复用。理论支撑：CEMR（arXiv 2603.08037）的 Lemma 3.1 证明了共享 backward neighbor 映射的扩展结果可复用。

#### 实现位置

修改 `session_pipeline.py` 中的逐层估计循环（Step 4），在每层开始前检查跳过条件。`score_aggregator.py` 中为被跳过的层记录占位值（如该层均值 $\bar{c}_k$）。

### 2.2 O2：位置-拓扑感知加权代价模型（Position-Topology Aware Weighting）

#### 核心思想

用参数化权重函数替代等权求和，使代价模型更准确地反映真实执行代价：

$$\text{Cost}(O) = \sum_{k \in \mathcal{K}} \omega(k, Q_k) \cdot \hat{c}_k$$

其中 $\mathcal{K}$ 是经 O1 筛选后保留的前缀索引集合，$\omega(k, Q_k)$ 是依赖于位置 $k$ 和前缀拓扑 $Q_k$ 的权重函数。

#### 权重函数设计

$$\omega(k, Q_k) = \alpha(k) \cdot \beta(Q_k)$$

**位置因子** $\alpha(k)$：

$$\alpha(k) = \left(\frac{n - k + 1}{n}\right)^\gamma$$

其中 $\gamma > 0$ 为衰减指数。$\gamma = 1$ 时为线性衰减，$\gamma > 1$ 时前部权重更集中。直觉：序列前部的前缀对执行时间影响更大。

**拓扑因子** $\beta(Q_k)$：

$$\beta(Q_k) = 1 + \lambda \cdot \frac{|E_k| - (|V_k| - 1)}{|V_k|}$$

其中 $|E_k|$ 和 $|V_k|$ 分别是 $Q_k$ 的边数和顶点数，$\lambda \geq 0$ 为拓扑敏感系数。当 $Q_k$ 是树时 $\beta = 1$；含环越多 $\beta$ 越大，表示该前缀的约束更强、估计值更有信息量。

#### 超参数

| 参数 | 含义 | 建议搜索范围 |
|------|------|------------|
| $\gamma$ | 位置衰减指数 | {0.5, 1.0, 1.5, 2.0} |
| $\lambda$ | 拓扑敏感系数 | {0.0, 0.5, 1.0, 2.0} |

#### 超参数调优方法

使用小规模标定实验确定最优超参数：

1. 选取 1 个数据集（如 yeast），生成 100 条查询（|V_q| = 8, 12 各 50 条）
2. 对每条查询，用 baseline M1 生成全部序列，M2 估计全部前缀
3. 将 Top-10 序列送入 M3 执行，记录真实执行时间
4. 对每组 $(\gamma, \lambda)$，用加权代价重新排序，计算加权排序的 Top-1 在 M3 中的排名
5. 选择使 Top-1 命中率最高的 $(\gamma, \lambda)$

#### 实现位置

修改 `score_aggregator.py:get_weight(k, n)` 为 `get_weight(k, n, n_edges_k, n_vertices_k)`，接受前缀拓扑信息。`session_pipeline.py` 在调用 `record_estimate` 时传入前缀的边数和顶点数。

### 2.3 O3：高代价查询预警与估计值替代（Cost-Based Query Advisory）

#### 核心思想

当 M2 估计值表明查询代价极高时，向用户发出预警，建议使用基数估计值替代真实查询执行，避免不必要的长时间等待。

#### 预警触发条件

定义代价阈值 $\tau$。当最优序列的代价满足以下任一条件时触发预警：

**C1 — 绝对阈值**：$\text{Cost}(O^*) > \tau_{\text{abs}}$

**C2 — 相对阈值**：$\hat{c}_n > \tau_{\text{rel}} \cdot |V_D|$（完整查询图的基数估计值超过数据图顶点数的 $\tau_{\text{rel}}$ 倍）

**C3 — 增长率阈值**：$\hat{c}_k / \hat{c}_{k-1} > \tau_{\text{growth}}$ 对某个 $k$（基数在某一层发生爆炸性增长）

#### 预警内容

通过 SSE 事件推送至前端：

```json
{
  "event": "cost_advisory",
  "data": {
    "severity": "warning",
    "estimated_cost": 1.2e8,
    "estimated_time_seconds": 120,
    "message": "查询基数估计值较大，下游执行可能耗时较长。建议使用基数估计值作为参考，跳过真实查询执行。",
    "estimated_cardinality": 1.2e8,
    "confidence": "high"
  }
}
```

#### 估计值可信度验证

需要通过实验证明基数估计值与真实值的接近程度（见 3.3 节 E-O3）。

注意：根据文献调研（见第 0 节），FaSTest 在特定数据集上精度很高（q-error ~1.1），但 G-CARE 基准（SIGMOD 2020）指出"所有现有技术在各种场景和数据集上都存在严重的精度问题"。因此：

1. 不应将基数估计值作为"精确结果数量"呈现给用户，而应定位为"数量级参考"和"查询难度指标"
2. 预警应优先使用 FaSTest 的 Clopper-Pearson 置信区间上界，而非点估计
3. 代价-时间映射应使用对数空间的幂律模型，而非线性假设（参考 arXiv 1905.00774 的发现）

可信度分级：

| 等级 | 条件（q-error） | 含义 |
|------|----------------|------|
| high | q-error < 2 | 估计值与真实值在同一数量级，偏差不超过 2 倍 |
| medium | q-error 2-10 | 估计值可作为数量级参考 |
| low | q-error > 10 | 仅供趋势参考，不建议替代真实执行 |

#### 渐进式验证（补充方案）

受 POP（SIGMOD 2004）和 LPCE（SIGMOD 2023）启发，可在 M3 执行早期阶段验证基数估计：

1. M3 开始执行后，在前 $t_0$ 秒（如 5s）内观察实际枚举速度
2. 外推预计总执行时间：$T_{\text{est}} = t_0 \cdot (\hat{c}_n / c_{\text{observed}})$
3. 若 $T_{\text{est}}$ 超过用户设定的时间限制，则提前终止并预警
4. 这比纯静态预警更可靠，因为它结合了实际执行信息

#### 实现位置

在 `session_pipeline.py` 的 Step 4（逐层估计）完成后、Step 5（下游执行）之前插入预警检查。新增 `CostAdvisor` 类负责阈值判断和预警事件生成。前端在 `useSessionStream.ts` 中监听 `cost_advisory` 事件并弹出提示。

## 3 实验设计

### 3.1 E-O1：选择性前缀估计的效率与无损性验证

#### E-O1a：估计调用缩减率

**目的**：量化 O1 各跳过规则节省的估计调用次数。

**方法**：

| 配置 | R1 恒等消除 | R2 低区分度 | R3 自适应跳过 |
|------|-----------|-----------|-------------|
| Full | ✓ | ✓ | ✓ |
| R1-only | ✓ | ✗ | ✗ |
| R1+R2 | ✓ | ✓ | ✗ |
| None（baseline） | ✗ | ✗ | ✗ |

对每个配置记录：

| 指标 | 说明 |
|------|------|
| `N_calls_total` | C++ 估计调用总次数 |
| `N_calls_skipped` | 被跳过的调用次数 |
| `T_M2` | M2 阶段总耗时 |
| `skip_ratio` | `N_calls_skipped / N_calls_total` |

**工作负载**：6 数据集 × |V_q| = {4, 8, 12, 16} × 30 查询 = 720 实验点

**输出**：
- 表格：各配置在不同 |V_q| 下的 skip_ratio 和 T_M2
- 折线图：横轴 |V_q|，纵轴 T_M2 节省比例

#### E-O1b：跳过对排序的影响

**目的**：验证跳过前缀后序列排序不变或变化可忽略。

**方法**：对每条查询，分别用 baseline（全估计）和 O1（选择性估计）计算代价排序，比较：

| 指标 | 说明 |
|------|------|
| `rank_correlation` | Spearman 秩相关系数（全序列排序） |
| `top1_match` | Top-1 序列是否一致 |
| `top5_overlap` | Top-5 序列的重叠数 |

**预期**：R1 和 R2 为精确无损（rank_correlation = 1.0）；R3 在合理 $\epsilon$ 下 top1_match > 95%。

#### E-O1c：自适应跳过阈值 $\epsilon$ 的敏感性

**方法**：固定 yeast 数据集，|V_q| = 12，变化 $\epsilon$：

| $\epsilon$ | skip_ratio | top1_match | rank_correlation |
|------------|-----------|------------|-----------------|
| 0.001 | | | |
| 0.01 | | | |
| 0.05 | | | |
| 0.1 | | | |
| 0.2 | | | |

### 3.2 E-O2：加权代价模型的有效性验证

#### E-O2a：加权排序 vs 等权排序的 M3 执行对比

**目的**：验证加权代价模型选出的 Top-1 序列在 M3 中的执行性能优于等权模型。

**方法**：

1. 对每条查询，用 M2 估计全部前缀
2. 分别用等权和加权（最优 $\gamma, \lambda$）计算代价排序
3. 将两种排序的 Top-1 序列送入 M3 执行

| 指标 | 说明 |
|------|------|
| `EPS_uniform_top1` | 等权 Top-1 的 M3 EPS |
| `EPS_weighted_top1` | 加权 Top-1 的 M3 EPS |
| `Time_uniform_top1` | 等权 Top-1 的 M3 执行时间 |
| `Time_weighted_top1` | 加权 Top-1 的 M3 执行时间 |
| `improvement` | `(Time_uniform - Time_weighted) / Time_uniform` |

**工作负载**：6 数据集 × |V_q| = {8, 12, 16} × 50 查询 = 900 实验点

**输出**：
- 配对箱线图：等权 vs 加权的 M3 执行时间分布
- 加速比直方图
- Wilcoxon 检验 p 值

#### E-O2b：超参数网格搜索

**目的**：确定 $\gamma$ 和 $\lambda$ 的最优组合。

**方法**：在标定集（yeast, |V_q| = {8, 12} × 50 查询）上做网格搜索：

| $\gamma$ \ $\lambda$ | 0.0 | 0.5 | 1.0 | 2.0 |
|----------------------|-----|-----|-----|-----|
| 0.5 | | | | |
| 1.0 | | | | |
| 1.5 | | | | |
| 2.0 | | | | |

每格记录：Top-1 命中率（加权 Top-1 落在 M3 真实 Top-3 内的比例）。

**输出**：热力图，颜色表示命中率。

#### E-O2c：加权模型的跨数据集泛化性

**目的**：验证在 yeast 上调优的超参数在其他数据集上是否仍然有效。

**方法**：用 yeast 上的最优 $(\gamma^*, \lambda^*)$，在其余 5 个数据集上评估 Top-1 命中率和 M3 执行时间改善。

### 3.3 E-O3：基数估计值可信度验证

#### E-O3a：估计值 vs 真实值的精度

**目的**：量化 FaSTest 基数估计值与 M3 真实嵌入数的偏差。

**方法**：对每条查询，记录：

| 指标 | 说明 |
|------|------|
| `c_hat` | M2 基数估计值（完整查询图 $Q_n$ 的 $\hat{c}_n$） |
| `c_real` | M3 真实嵌入数（`embedding_count`） |
| `relative_error` | `|c_hat - c_real| / c_real` |
| `q_ratio` | `c_hat / c_real`（>1 为高估，<1 为低估） |

**工作负载**：6 数据集 × |V_q| = {4, 8, 12} × 50 查询 = 900 实验点

**输出**：
- 散点图：横轴 $c_{\text{real}}$（对数刻度），纵轴 $c_{\hat{}}$（对数刻度），期望沿 y=x 分布
- 相对误差的分布直方图
- 按 $c_{\text{real}}$ 分桶的中位相对误差（验证大基数时估计是否仍准确）
- 可信度等级分布：high / medium / low 各占比

#### E-O3b：预警阈值标定

**目的**：确定合理的预警阈值 $\tau$，使得预警既不过于频繁（误报）也不遗漏高代价查询（漏报）。

**方法**：

定义"高代价查询"为 M3 执行时间 > 30s 的查询。对不同阈值 $\tau_{\text{abs}}$，计算：

| $\tau_{\text{abs}}$ | 精确率 | 召回率 | F1 |
|---------------------|--------|--------|-----|
| 1e4 | | | |
| 1e5 | | | |
| 1e6 | | | |
| 1e7 | | | |
| 1e8 | | | |

- 精确率 = 触发预警且确实高代价的查询数 / 触发预警的查询数
- 召回率 = 触发预警且确实高代价的查询数 / 实际高代价的查询数

**输出**：PR 曲线，选择 F1 最大的阈值作为默认值。

## 4 三项优化的协同关系

```
O1（选择性前缀估计）
  │
  │ 减少估计调用次数 → 降低 T_M2
  │ 输出：保留的前缀索引集合 K
  │
  ▼
O2（加权代价模型）
  │
  │ 仅对 K 中的前缀计算加权代价 → 更准确的排序
  │ 输出：加权排序后的最优序列 O*，以及 Cost(O*)
  │
  ▼
O3（高代价预警）
  │
  │ 若 Cost(O*) > τ → 预警，建议使用估计值替代
  │ 否则 → 正常送入 M3 执行
  ▼
```

三者形成递进关系：O1 减少计算量，O2 提升排序质量，O3 在代价过高时提供安全出口。它们可以独立开启或组合使用。

## 5 实现优先级

| 优先级 | 优化项 | 理由 |
|--------|--------|------|
| P0 | O1-R1（末尾前缀消除） | 零风险，直接节省 $|\Omega|$ 次调用 |
| P0 | O1-R4（前缀 Memoization） | 零风险，跨序列复用估计结果，收益可能最大 |
| P1 | O2（位置衰减加权） | 文献空白，有创新价值，先实现简单版本验证 |
| P2 | O3（预警机制 + 渐进式验证） | 用户体验改善，需先完成 E-O3a 精度实验确定阈值 |
| P3 | O1-R3（自适应跳过） | 需要仔细调参，有排序偏移风险 |

## 6 参考文献

| 缩写 | 全称 | 出处 |
|------|------|------|
| FaSTest | Cardinality Estimation of Subgraph Matching: A Filtering-Sampling Approach | arXiv 2309.15433 |
| NeuSO | Neural Optimizer for Subgraph Queries | arXiv 2509.23775 |
| CEMR | An Effective Subgraph Matching Algorithm with Redundant Extension Elimination | arXiv 2603.08037 |
| DAF | Efficient Subgraph Matching: Harmonizing DP, Adaptive Matching Order, and Failing Set | SIGMOD 2019 |
| G-CARE | A Framework for Cardinality Estimation in Graph Databases | SIGMOD 2020 |
| LpBound | Pessimistic Cardinality Estimation | SIGMOD 2025 Best Paper |
| POP | Progressive Optimization of Queries | SIGMOD 2004 |
| LPCE | Learned Progressive Cardinality Estimation | SIGMOD 2023 |
| COLOR | Graph Coloring for Subgraph Cardinality Estimation | arXiv 2405.06767 |
| RL-Ordering | Reinforcement Learning Based Query Vertex Ordering Model | 2022 |
| NeuGN | Neural Graph Navigation for Subgraph Matching | arXiv 2511.17939 |
